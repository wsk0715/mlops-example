## Spec

- main.ts: createApp -> use(vuetify) -> use(router) -> use(pinia) -> mount
- App.vue: v-app -> v-app-bar(AppBar) + v-navigation-drawer(SideNav) + v-main(router-view)
- plugins/vuetify.ts: createVuetify with components + directives

## Completion

- [ ] npm run dev -> 빈 페이지 정상 렌더링
- [ ] console error 없음
